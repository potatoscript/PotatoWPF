<Window x:Class="PotatoWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" 
        WindowStartupLocation="CenterScreen"
        MaxHeight="750" MaxWidth="1000"
        MinHeight="750" MinWidth="1000"
        WindowStyle="SingleBorderWindow">
    <DockPanel>
        <!-- Menu -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Home" />
            <MenuItem Header="User">
                <MenuItem Header="Profile" Click="ProfileMenuItem_Click" />
                <MenuItem Header="Settings" />
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" />
                <MenuItem Header="Support" />
            </MenuItem>
        </Menu>

        <!-- Content Area -->
        <Grid Name="MainContentGrid">
            <TextBlock Text="Welcome to PotatoScript WPF Project" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="20"/>
        </Grid>
    </DockPanel>
</Window>
